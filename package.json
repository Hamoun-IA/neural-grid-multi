{
  "name": "neural-grid-mono",
  "version": "1.0.0",
  "description": "Cyberpunk 3D dashboard for monitoring OpenClaw agent infrastructure",
  "private": true,
  "scripts": {
    "dev:frontend": "cd frontend && npm run dev -- --port 3100 --host 0.0.0.0",
    "dev:backend": "cd backend && npm run dev",
    "install:all": "cd frontend && npm install && cd ../backend && npm install",
    "build:frontend": "cd frontend && npm run build",
    "build:backend": "cd backend && npm run build"
  },
  "workspaces": [
    "frontend",
    "backend"
  ]
}
